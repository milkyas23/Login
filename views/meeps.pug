extends layout 

block content 

  each meep in meeps 
    div
      p= meep.body 
      p by: #{meep.author}
      form(action="/meeps/delete", method="post") 
        input(type="hidden", name="meepid" value= meep.id)
        button.btn.btn-outline-primary(type="submit") X