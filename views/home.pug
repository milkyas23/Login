extends layout

block content
  main.container.pt-5
    h1 Welcome back #{username}

    form(action="/logout", method="post")
      //- vi låtsas vara en delete request
      button.btn.btn-outline-primary(type="submit") Logout

    h2 Change password or username
    ul
      li Skapa tests om du så önskar 
      li Skapa en update metod i authcontroller 
      li posta data från formulär här till /home/:include
      li home router för post på id

    h2 Delete user 
    p Post till /home/userid med password för säkerhet, eller länk till /home/delete/userid

    h2 Ladda upp en avatar 
    p spara i databasen / spara i en temp mapp 
      a(href="https://www.npmjs.com/package/express-fileupload") Express fileupload